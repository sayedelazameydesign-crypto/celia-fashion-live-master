# 🛍️ متجر الملابس الذكي - EcommerceAI3D

متجر إلكتروني تفاعلي لبيع الملابس مع عرض ثلاثي الأبعاد وتوصيات ذكية مدعومة بالذكاء الاصطناعي.

## ✨ المميزات

### 🎯 للمستخدمين
- **عرض ثلاثي الأبعاد** تفاعلي للمنتجات باستخدام Three.js
- **توصيات ذكية** بناءً على تفضيلاتك باستخدام scikit-learn
- **تجربة تصفح سلسة** مع أنيميشن GSAP
- **دعم كامل للغة العربية** في الواجهة
- **بحث متقدم** عن المنتجات والمقالات
- **مدونة مدمجة** لمقالات الموضة والنصائح

### 🔧 للمدراء
- **لوحة تحكم شاملة** لإدارة المتجر
- **إضافة/تعديل/حذف المنتجات** بسهولة
- **إدارة المقالات** الكاملة مع محرر HTML
- **نظام SEO متكامل** لكل منتج ومقال
- **إحصائيات فورية** عن المنتجات والمقالات

## 🏗️ التقنيات المستخدمة

### Backend
- **Flask 3.0** - إطار عمل Python
- **SQLAlchemy** - ORM لقاعدة البيانات
- **PostgreSQL** - قاعدة بيانات (Supabase)
- **scikit-learn** - توصيات ذكية بالـ AI

### Frontend
- **Three.js** - عرض 3D تفاعلي
- **Tailwind CSS** - تصميم عصري
- **GSAP** - أنيميشن احترافي
- **HTML5 + JavaScript** - واجهة تفاعلية

### استضافة مجانية 100%
- **Render** - Backend hosting
- **Supabase** - قاعدة بيانات PostgreSQL
- **Cloudinary** - تخزين الصور والنماذج 3D

---

## 🚀 دليل التثبيت والنشر

### 1️⃣ المتطلبات الأساسية

```bash
# Python 3.9 أو أحدث
python --version

# Git
git --version
```

### 2️⃣ إعداد المشروع محلياً

```bash
# 1. استنساخ المشروع
git clone https://github.com/your-username/EcommerceAI3D.git
cd EcommerceAI3D

# 2. إنشاء بيئة افتراضية
python -m venv venv

# تفعيل البيئة (Windows)
venv\Scripts\activate

# تفعيل البيئة (Mac/Linux)
source venv/bin/activate

# 3. تثبيت المتطلبات
pip install -r requirements.txt

# 4. نسخ ملف البيئة
cp .env.example .env

# 5. تعديل ملف .env بمعلومات قاعدة البيانات الخاصة بك
```

### 3️⃣ إعداد Supabase (قاعدة البيانات المجانية)

1. **إنشاء حساب**: اذهب إلى [Supabase.com](https://supabase.com)
2. **مشروع جديد**: اضغط "New Project"
3. **معلومات المشروع**:
   - اسم المشروع: `ecommerce-ai3d`
   - كلمة مرور قوية (احفظها!)
   - المنطقة: اختر الأقرب لك
4. **الحصول على رابط الاتصال**:
   - اذهب إلى Settings → Database
   - انسخ "Connection String" (URI)
   - استبدل `[YOUR-PASSWORD]` بكلمة المرور
5. **ضع الرابط في `.env`**:
   ```
   DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.xxxxx.supabase.co:5432/postgres
   ```

### 4️⃣ إعداد Cloudinary (تخزين الملفات المجاني)

1. **إنشاء حساب**: اذهب إلى [Cloudinary.com](https://cloudinary.com)
2. **الحصول على المعلومات**:
   - Dashboard → Account Details
   - انسخ: Cloud Name, API Key, API Secret
3. **ضعها في `.env`**:
   ```
   CLOUDINARY_CLOUD_NAME=your-cloud-name
   CLOUDINARY_API_KEY=your-api-key
   CLOUDINARY_API_SECRET=your-api-secret
   ```

### 5️⃣ تشغيل المشروع محلياً

```bash
# تشغيل التطبيق
python run.py

# افتح المتصفح على
http://localhost:5000
```

### 6️⃣ تحميل بيانات تجريبية

1. افتح `http://localhost:5000/admin`
2. اضغط "تحميل بيانات تجريبية" للمنتجات
3. اضغط "تحميل مقالات تجريبية" للمقالات

---

## 🌐 النشر على Render (مجاني)

### الطريقة 1: النشر من GitHub (موصى بها)

#### خطوة 1: رفع المشروع على GitHub

```bash
# 1. إنشاء repository على GitHub
# اذهب إلى github.com وأنشئ repository جديد

# 2. ربط المشروع بـ GitHub
git init
git add .
git commit -m "Initial commit: E-commerce AI 3D Store"
git remote add origin https://github.com/your-username/EcommerceAI3D.git
git push -u origin main
```

#### خطوة 2: النشر على Render

1. **إنشاء حساب**: اذهب إلى [Render.com](https://render.com)
2. **Web Service جديد**:
   - Dashboard → New → Web Service
   - Connect GitHub repository
   - اختر repository: `EcommerceAI3D`
3. **الإعدادات**:
   - **Name**: `ecommerce-ai3d`
   - **Environment**: Python 3
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `gunicorn run:app`
   - **Plan**: Free
4. **متغيرات البيئة** (Environment Variables):
   ```
   SECRET_KEY = your-secret-key-123456
   DATABASE_URL = postgresql://... (من Supabase)
   CLOUDINARY_CLOUD_NAME = your-cloud-name
   CLOUDINARY_API_KEY = your-api-key
   CLOUDINARY_API_SECRET = your-api-secret
   FLASK_ENV = production
   ```
5. **النشر**: اضغط "Create Web Service"
6. **انتظر**: 5-10 دقائق للنشر

#### خطوة 3: الوصول للموقع

موقعك سيكون على: `https://ecommerce-ai3d.onrender.com`

---

## 📁 هيكل المشروع

```
EcommerceAI3D/
│
├── app/
│   ├── __init__.py           # تهيئة التطبيق
│   ├── models/               # نماذج قاعدة البيانات
│   │   ├── product.py        # المنتجات، المستخدمين، الطلبات
│   │   └── article.py        # المقالات
│   ├── routes/               # المسارات
│   │   ├── main.py           # الصفحات الرئيسية
│   │   ├── products.py       # صفحات المنتجات
│   │   ├── ai_routes.py      # API التوصيات
│   │   ├── admin.py          # لوحة التحكم
│   │   └── blog.py           # المدونة
│   ├── utils/                # أدوات مساعدة
│   │   └── ai_recommender.py # نظام التوصيات
│   ├── templates/            # صفحات HTML
│   │   ├── layout.html       # القالب الرئيسي
│   │   └── admin/            # صفحات لوحة التحكم
│   └── static/               # ملفات ثابتة
│       ├── css/
│       ├── js/
│       ├── models/           # نماذج 3D (.glb)
│       └── images/
│
├── config.py                 # إعدادات التطبيق
├── requirements.txt          # متطلبات Python
├── run.py                    # ملف التشغيل
├── Procfile                  # لـ Render
└── .env.example              # مثال للمتغيرات
```

---

## 🎨 استخدام لوحة التحكم

### الوصول للوحة التحكم
```
http://your-site.com/admin
```

### إدارة المنتجات

#### إضافة منتج جديد
1. لوحة التحكم → إضافة منتج جديد
2. املأ الحقول:
   - الاسم، الوصف، السعر، الفئة
   - اللون، الكمية
   - رابط نموذج 3D (من Cloudinary)
   - صورة مصغرة
3. ✅ اختر "منتج مميز" إذا أردت ظهوره في الصفحة الرئيسية
4. احفظ

#### تعديل منتج
1. لوحة التحكم → إدارة المنتجات
2. اضغط زر "تعديل" بجانب المنتج
3. عدل المعلومات
4. احفظ

#### حذف منتج
1. لوحة التحكم → إدارة المنتجات
2. اضغط زر "حذف" بجانب المنتج
3. أكد الحذف

### إدارة المقالات

#### كتابة مقال جديد
1. لوحة التحكم → كتابة مقال جديد
2. املأ الحقول:
   - العنوان، المحتوى، المقتطف
   - الفئة، اسم الكاتب
   - صورة بارزة، الوسوم
   - وصف SEO، كلمات مفتاحية
3. ✅ اختر "نشر المقال" للنشر مباشرة
4. ✅ اختر "مقال مميز" للظهور في الصفحة الرئيسية
5. احفظ

#### تعديل مقال
1. لوحة التحكم → إدارة المقالات
2. اضغط زر "تعديل" بجانب المقال
3. عدل المحتوى
4. احفظ

#### حذف مقال
1. لوحة التحكم → إدارة المقالات
2. اضغط زر "حذف"
3. أكد الحذف

---

## 🎭 رفع نماذج 3D على Cloudinary

### 1. تحميل نموذج 3D

**الحصول على نماذج مجانية:**
- [Sketchfab](https://sketchfab.com) - ابحث عن "clothing 3d"
- [Free3D](https://free3d.com)
- [TurboSquid Free](https://www.turbosquid.com/Search/3D-Models/free)

**التنسيقات المدعومة:** `.glb` (الأفضل), `.gltf`

### 2. رفع على Cloudinary

```bash
# طريقة 1: من لوحة Cloudinary
# 1. اذهب إلى dashboard.cloudinary.com
# 2. Media Library → Upload
# 3. اختر الملف .glb
# 4. انسخ الرابط

# طريقة 2: من Python
from cloudinary.uploader import upload

result = upload("path/to/model.glb", resource_type="raw")
print(result['secure_url'])  # انسخ هذا الرابط
```

### 3. استخدام الرابط
- ضع الرابط في حقل "رابط نموذج 3D" عند إضافة المنتج
- مثال: `https://res.cloudinary.com/your-cloud/raw/upload/v1234/model.glb`

---

## 🧪 اختبار المشروع

### اختبار محلي

```bash
# تشغيل في وضع التطوير
python run.py

# اختبار الصفحات
# الرئيسية: http://localhost:5000
# لوحة التحكم: http://localhost:5000/admin
# المدونة: http://localhost:5000/blog
```

### اختبار التوصيات

```bash
# افتح Python shell
python

>>> from app import create_app, db
>>> from app.utils.ai_recommender import recommender
>>> app = create_app()
>>> with app.app_context():
...     recommendations = recommender.recommend_by_similarity(1, limit=4)
...     print(recommendations)
```

---

## 🔧 تخصيص المشروع

### تغيير الألوان

عدل في `app/templates/layout.html`:

```html
<style>
    .gradient-bg {
        /* غير الألوان هنا */
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    }
</style>
```

### إضافة فئة منتجات جديدة

1. افتح `app/templates/admin/article_form.html`
2. أضف option جديد في قائمة الفئات:
```html
<option value="new-category">الفئة الجديدة</option>
```

### تخصيص التوصيات

عدل في `app/utils/ai_recommender.py` → دالة `recommend_by_similarity()`

---

## 📊 الإحصائيات والمتابعة

### Google Analytics
1. أنشئ حساب على [analytics.google.com](https://analytics.google.com)
2. احصل على Tracking ID
3. أضفه في `layout.html` قبل `</head>`:

```html
<!-- Google Analytics -->
<script async src="https://www.googletagmanager.com/gtag/js?id=G-XXXXXXXXXX"></script>
<script>
  window.dataLayer = window.dataLayer || [];
  function gtag(){dataLayer.push(arguments);}
  gtag('js', new Date());
  gtag('config', 'G-XXXXXXXXXX');
</script>
```

---

## 🐛 حل المشاكل الشائعة

### مشكلة: قاعدة البيانات لا تعمل
```bash
# تأكد من رابط DATABASE_URL صحيح
# تأكد من استبدال [YOUR-PASSWORD] بكلمة المرور الفعلية
```

### مشكلة: الصور لا تظهر
```bash
# تأكد من إعدادات Cloudinary في .env
# تأكد من رفع الصور على Cloudinary
```

### مشكلة: نموذج 3D لا يظهر
```bash
# تأكد من أن الملف بصيغة .glb
# تأكد من أن الرابط صحيح
# افتح Console في المتصفح لرؤية الأخطاء
```

---

## 📝 To-Do List

- [ ] إضافة نظام تسجيل الدخول
- [ ] إضافة سلة مشتريات حقيقية
- [ ] دمج بوابة دفع (Stripe/PayPal)
- [ ] إضافة نظام تقييمات
- [ ] تحسين محرك التوصيات بـ ML
- [ ] إضافة لوحة إحصائيات متقدمة
- [ ] دعم لغات متعددة

---

## 🤝 المساهمة

المساهمات مرحب بها! 

1. Fork المشروع
2. أنشئ branch جديد (`git checkout -b feature/amazing-feature`)
3. Commit تغييراتك (`git commit -m 'Add amazing feature'`)
4. Push للـ branch (`git push origin feature/amazing-feature`)
5. افتح Pull Request

---

## 📄 الترخيص

هذا المشروع مفتوح المصدر تحت رخصة MIT.

---

## 📧 التواصل

- **المطور**: MiniMax Agent
- **البريد**: info@ecommerce-ai3d.com
- **الموقع**: https://your-site.onrender.com

---

## 🎉 شكر خاص

- **Three.js** - مكتبة 3D رائعة
- **Tailwind CSS** - أفضل CSS framework
- **Flask** - إطار عمل Python المرن
- **Supabase** - قاعدة بيانات مجانية وسخية
- **Cloudinary** - تخزين ملفات ممتاز
- **Render** - استضافة مجانية سريعة

---

Made with ❤️ by MiniMax Agent
